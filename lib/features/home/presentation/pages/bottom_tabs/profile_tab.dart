import 'package:flutter/material.dart';

class ProfileTab extends StatelessWidget {
  const ProfileTab({super.key, required userEntity});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
