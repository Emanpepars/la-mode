import 'package:flutter/material.dart';

class FilterSaleTab extends StatelessWidget {
  const FilterSaleTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ًSale content'),
    );
  }
}
