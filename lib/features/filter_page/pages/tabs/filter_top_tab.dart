import 'package:flutter/material.dart';

class FilterTopTab extends StatelessWidget {
  const FilterTopTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Top content',
      ),
    );
  }
}
